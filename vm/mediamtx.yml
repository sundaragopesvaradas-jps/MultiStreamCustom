# MediaMTX — Zoom ingest + fan-out hooks
# Docs: https://github.com/bluenviron/mediamtx

logLevel: info
logDestinations: [stdout]

rtmp: yes
rtmpAddress: :1935

# Disable unused protocols to reduce attack surface
rtsp: no
hls: no
webrtc: no
srt: no

api: yes
apiAddress: 127.0.0.1:9997

# Open publish/read locally; fan-out script enforces ingest key (live/<key>).
authMethod: internal
authInternalUsers:
  - user: any
    pass:
    permissions:
      - action: publish
      - action: read
      - action: api

pathDefaults:
  runOnReady: /opt/multistream/bin/on-stream-ready.sh $MTX_PATH
  runOnReadyRestart: yes
  runOnNotReady: /opt/multistream/bin/on-stream-end.sh $MTX_PATH

paths:
  all:
